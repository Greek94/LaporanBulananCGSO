# Print Layout Enhancement

This note documents the A4 print layout hardening for Secretariat read-only reports.

The dashboard remains browser-based and uses the browser print dialog. The print CSS is optimized for A4 paper, reduced margins, compact typography, repeated table headers, and reduced page-break fragmentation.
